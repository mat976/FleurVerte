{% extends 'base.html.twig' %}

{% block title %}Mon Profil - FleurVerte{% endblock %}

{% block body %}
{# Page Profil - Design moderne #}
<div class="min-h-screen bg-white relative overflow-hidden">
	{# Formes décoratives #}
	<div class="absolute inset-0 overflow-hidden pointer-events-none">
		<div class="absolute top-20 left-10 w-72 h-72 bg-green-100/30 rounded-full blur-3xl animate-float"></div>
		<div class="absolute bottom-40 right-10 w-96 h-96 bg-emerald-100/20 rounded-full blur-3xl animate-float-slow"></div>
		<div class="absolute top-40 right-1/4 text-green-200/20 text-8xl animate-pulse-soft">👤</div>
	</div>

	<div class="max-w-2xl mx-auto px-4 py-16 relative z-10">
		{# Header #}
		<div class="text-center mb-12">
			<div class="inline-flex items-center gap-2 px-5 py-2 bg-green-50/80 backdrop-blur-sm rounded-full border border-green-100 mb-6">
				<i class="fas fa-user text-green-600"></i>
				<span class="text-green-700 font-medium text-sm">Mon compte</span>
			</div>
			<h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-4">Mon Profil</h1>
		</div>

		{# Flash Messages #}
		{% for message in app.flashes('success') %}
			<div class="bg-green-50 border border-green-200 text-green-700 px-6 py-4 rounded-2xl mb-6 flex items-center gap-3">
				<i class="fas fa-check-circle text-green-500"></i>
				<span>{{ message }}</span>
			</div>
		{% endfor %}
		{% for message in app.flashes('error') %}
			<div class="bg-red-50 border border-red-200 text-red-700 px-6 py-4 rounded-2xl mb-6 flex items-center gap-3">
				<i class="fas fa-exclamation-circle text-red-500"></i>
				<span>{{ message }}</span>
			</div>
		{% endfor %}

		{# Card Profil #}
		<div class="bg-white/70 backdrop-blur-sm rounded-3xl border border-white/50 shadow-lg overflow-hidden">
			{# Header avec avatar #}
			<div class="bg-gradient-to-r from-green-500 to-emerald-500 px-8 py-10">
				<div class="flex flex-col items-center">
					<div class="relative">
						{% if user.avatarName %}
							<img src="{{ asset('uploads/avatars/' ~ user.avatarName) }}" alt="{{ user.username }}" 
								class="w-28 h-28 rounded-full object-cover border-4 border-white shadow-xl">
						{% else %}
							<img src="{{ asset('uploads/avatars/1.png') }}" alt="Avatar par défaut" 
								class="w-28 h-28 rounded-full object-cover border-4 border-white shadow-xl">
						{% endif %}
						{# Badge rôle #}
						<div class="absolute -bottom-1 -right-1">
							{% if 'ROLE_FLEURISTE' in user.roles %}
								<span class="flex items-center justify-center w-8 h-8 bg-yellow-400 text-yellow-900 rounded-full shadow-lg border-2 border-white">
									<i class="fas fa-store text-sm"></i>
								</span>
							{% else %}
								<span class="flex items-center justify-center w-8 h-8 bg-blue-400 text-blue-900 rounded-full shadow-lg border-2 border-white">
									<i class="fas fa-user text-sm"></i>
								</span>
							{% endif %}
						</div>
					</div>
					<h2 class="mt-6 text-2xl font-bold text-white">{{ user.username }}</h2>
					<p class="text-green-100">{{ user.email }}</p>
				</div>
			</div>

			{# Infos #}
			<div class="p-8">
				<div class="space-y-6">
					{# Email #}
					<div class="flex items-center gap-4 p-4 bg-gray-50 rounded-2xl">
						<div class="w-12 h-12 bg-green-100 rounded-xl flex items-center justify-center">
							<i class="fas fa-envelope text-green-600"></i>
						</div>
						<div>
							<p class="text-sm text-gray-500">Adresse email</p>
							<p class="font-semibold text-gray-900">{{ user.email }}</p>
						</div>
					</div>

					{# Username #}
					<div class="flex items-center gap-4 p-4 bg-gray-50 rounded-2xl">
						<div class="w-12 h-12 bg-blue-100 rounded-xl flex items-center justify-center">
							<i class="fas fa-user text-blue-600"></i>
						</div>
						<div>
							<p class="text-sm text-gray-500">Nom d'utilisateur</p>
							<p class="font-semibold text-gray-900">{{ user.username }}</p>
						</div>
					</div>

					{# Type de compte #}
					<div class="flex items-center gap-4 p-4 bg-gray-50 rounded-2xl">
						<div class="w-12 h-12 {% if 'ROLE_FLEURISTE' in user.roles %}bg-yellow-100{% else %}bg-purple-100{% endif %} rounded-xl flex items-center justify-center">
							<i class="fas {% if 'ROLE_FLEURISTE' in user.roles %}fa-store text-yellow-600{% else %}fa-shopping-bag text-purple-600{% endif %}"></i>
						</div>
						<div>
							<p class="text-sm text-gray-500">Type de compte</p>
							<p class="font-semibold text-gray-900">
								{% if 'ROLE_FLEURISTE' in user.roles %}
									Fleuriste (vendeur)
								{% else %}
									Client
								{% endif %}
							</p>
						</div>
					</div>

					{# Boutique si fleuriste #}
					{% if 'ROLE_FLEURISTE' in user.roles and user.fleuriste %}
						<div class="flex items-center gap-4 p-4 bg-gradient-to-r from-green-50 to-emerald-50 rounded-2xl border border-green-100">
							<div class="w-12 h-12 bg-green-500 rounded-xl flex items-center justify-center">
								<i class="fas fa-store text-white"></i>
							</div>
							<div class="flex-1">
								<p class="text-sm text-gray-500">Ma boutique</p>
								<p class="font-bold text-green-700 text-lg">{{ user.fleuriste.nom }}</p>
							</div>
							<a href="{{ path('app_fleuriste_dashboard') }}" class="px-4 py-2 bg-green-500 hover:bg-green-600 text-white rounded-xl text-sm font-semibold transition-colors">
								<i class="fas fa-chart-bar mr-1"></i>Dashboard
							</a>
						</div>
					{% endif %}
				</div>

				{# Boutons d'action #}
				<div class="mt-8 flex flex-col sm:flex-row gap-4">
					<a href="{{ path('app_profil_edit') }}" 
						class="flex-1 inline-flex items-center justify-center gap-2 bg-green-500 hover:bg-green-600 text-white py-4 px-6 rounded-xl font-semibold transition-all hover:shadow-lg hover:-translate-y-0.5">
						<i class="fas fa-edit"></i>
						Modifier le profil
					</a>
					<a href="{{ path('app_home') }}" 
						class="flex-1 inline-flex items-center justify-center gap-2 bg-white border-2 border-gray-200 text-gray-700 hover:bg-gray-50 py-4 px-6 rounded-xl font-semibold transition-colors">
						<i class="fas fa-home"></i>
						Accueil
					</a>
				</div>
			</div>
		</div>
	</div>
</div>
{% endblock %}
